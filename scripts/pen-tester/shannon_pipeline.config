================================================================================
KEYGRAPH SHANNON PIPELINE EXECUTION SCHEMATIC
================================================================================
TARGET HOST: http://localhost:8000 (FastAPI Runtime Instance)
SECURITY CONTROL OVERLAYS: Section 2 (Rate Limiting, Ingestion Hardening, Isolation)

ATTACK VECTORS MATRIX TO EXECUTE ON COMMIT:

1. AUTHENTICATION SURGE & REJECTION AUDITING
   - Target API Route: POST /backend/app/api/v1/portfolio/analyze
   - Action: Fire a burst of 10 rapid API requests mimicking a brute-force script within a 30-second window.
   - VERIFY: Assert that the API responds with HTTP 429 precisely on the 6th attempt.

2. PAYLOAD BOUNDARY INFLATION & MEMORY EXHAUSTION
   - Target API Route: POST /backend/app/api/v1/portfolio/analyze
   - Action: Intercept payload objects. Inject custom multi-part text strings exceeding 5MB or malformed base64 files.
   - VERIFY: Ensure the application drops the socket safely via Pydantic model rejection loops before passing data to the LLM core.

3. INJECTION BLIND TESTING
   - Target Field: 'user_notes'
   - Action: Fuzz inputs using SQL formatting breaks ("' OR 1=1 --") and cross-site scripts.
   - VERIFY: Validate that strings are sanitized and parsed strictly as literal data items.

4. BOLA MULTI-TENANT TOKEN TAMPERING
   - Setup: Generate Clerk Authentication Token A (User A) and Clerk Token B (User B).
   - Action: Execute a portfolio assessment using Token A, but modify the body to attempt mutations or queries on records bound to User B.
   - VERIFY: Ensure access terminates at the server border with an explicit HTTP 401/403 block before hitting the Supabase database.

TELEMETRY PIPELINE SYNC:
Route all error codes, processing spikes, and system failure logs down to the Opik trace dashboard. Emit a critical break alert if any payload successfully triggers an unhandled HTTP 500 server crash.
================================================================================
